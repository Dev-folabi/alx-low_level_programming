#include "main.h"

/**
* positive_or_negative -cheack positive or negative header
*
* @i: the number to cheack
*
* Return: void
*/

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);

}
